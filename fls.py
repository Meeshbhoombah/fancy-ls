#!/usr/bin/env python
# -*- encoding: utf-8 -*-

"""
fls.py

Terminal command that expands on the functionality of the `ls` command by grouping files by 
their tags.

Attributes:
    TAGS_REF (string): the url of the `.plist` file containg the user's tags, and relevant tag 
        info.
"""

import os
import sys
import glob
import subprocess

# import plistlib
# TAGS_REF = "~/Library/SyncedPreferences/com.apple.finder.plist"

class TaggedObject:
        

if __name__ == "__main__":
    main(sys.argv)    

